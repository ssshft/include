系统：
绑核
json解析替换为simodjson；寻址解析，找对应字符串
行情解析
ssl魔改
多路websocket
网卡接收数据时间
dns优选ip，定时探测（缓存所有的ip），行情有效，交易不一定（ip近，离撮合引擎有可能远）；可能不同币对连接不同的ip，行情速度不一样
不同的ws连接走的是不同的网络，维持多个连接很有必要

通过下个特定价格的单，查出自己所在的耗时

cpprest 返回extract_json修改为extract_string(),再使用高性能json解析库（自带的json解析很慢）
cpprest修改为boost::beast

smc 合约信息，写到共享内存，不用单独开线程定时更新

检测服务器最快的，直接上去下单测试

硬件：


网络：
cdn cloudfire 背后的ip，最快的



交易所公告爬取：
api更新
下架上架

gate 东京a区，c8i, 16C32G
